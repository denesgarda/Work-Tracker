{
  "name": "work-tracker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 migrations apply work-tracker --local",
    "db:remote": "wrangler d1 migrations apply work-tracker --remote",
    "test": "TZ=America/New_York node test/stats.test.mjs && TZ=America/New_York node test/expenses.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.129.1"
  }
}